{
  "name": "team-pilled",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && cp manifest.json dist/manifest.json",
    "dev": "vite build --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.29",
    "jsdom": "^26.0.0",
    "typescript": "^5.7.3",
    "vite": "^8.0.16",
    "vitest": "^4.1.8"
  }
}
